{
  "name": "@momentiq/dark-factory-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Workspaces shell for @momentiq/dark-factory-cli and @momentiq/dark-factory-schemas",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@momentiq/dark-factory-schemas && npm run build --workspace=@momentiq/dark-factory-cli",
    "type-check": "npm run type-check --workspace=@momentiq/dark-factory-schemas && npm run type-check --workspace=@momentiq/dark-factory-cli",
    "test": "npm test --workspace=@momentiq/dark-factory-schemas && npm test --workspace=@momentiq/dark-factory-cli"
  },
  "engines": {
    "node": ">=20"
  }
}
